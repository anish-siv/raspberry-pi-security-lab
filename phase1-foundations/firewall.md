# Firewall Configuration

## Tool Used
- UFW (Uncomplicated Firewall)

## Configuration
- Enabled firewall
- Allowed SSH (port 22) explicitly
- Blocked all other incoming traffic by default

## Security Value
A host-based firewall reduces the attack surface by limiting which services are exposed to the network.
